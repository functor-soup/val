{
  "name": "example",
  "version": "1.0.0",
  "description": "This example shows show how to use val to log the logging stream from postfix into mongo.",
  "main": "example.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "bluebird": "^3.4.7",
    "mongodb": "^2.2.24"
  }
}
